# Verification notes

The unit and integration test files both pass. Their expected values come from `shared_oracle.expected_wire`.

No legacy binary, archived legacy payload, protocol specification, or independently maintained golden fixture is available in this repository.
