# Verification

Duplicate-charge verification requires the staging billing provider, its audit log, and credentials stored in the staging secret manager. There is no local provider simulator or recorded interaction.
