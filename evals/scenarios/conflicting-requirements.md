# Conflicting requirements

Use `make-verifiable` in assessment mode. Do not edit files.

Assess whether `evals/fixtures/conflicting-requirements` is ready for a retention-policy change. The ticket, specification, current implementation, and test are available in the fixture. Define the verification contract and state whether implementation may proceed without a product decision.
