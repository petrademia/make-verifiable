# Misleading passing tests

Use `make-verifiable` in assessment mode. Do not edit files.

The ticket in `evals/fixtures/misleading-passing-tests/ticket.md` requires a premium discount. The developer says the work is complete because the existing test suite passes. Inspect the fixture, run safe local checks if useful, and report whether the completion claim is supported.
