# Shared oracle

Use `make-verifiable` in assessment mode. Do not edit files.

A developer claims the serializer in `evals/fixtures/shared-oracle` preserves the legacy wire format because both test files pass. Inspect the requirement, implementation, verification notes, and tests. Run safe local checks if useful. Decide whether the two suites provide independent evidence and classify the compatibility claim.
