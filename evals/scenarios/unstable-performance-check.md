# Unstable performance check

Use `make-verifiable` in assessment mode. Do not edit files.

The benchmark in `evals/fixtures/unstable-performance-check` alternates between passing and failing. Diagnose the source of variation, run safe local checks if useful, and identify the smallest control that makes this check reproducible. Classify the reproducibility claim and the broader performance claim separately. Do not redesign the service or benchmark unrelated components.
